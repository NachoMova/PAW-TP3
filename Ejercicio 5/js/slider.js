var window = window || {},
    document = document || {},
    console = console || {},
    Slider = Slider || {};

window.onload = function() {
  Slider.init("slider");
};


Slider = {

  init: function () {

  }
}
